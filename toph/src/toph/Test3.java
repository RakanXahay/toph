// 声明一个名为toph的包  
package toph;  
  
// 声明一个名为Test3的公共类  
public class Test3 {  
    // 定义主方法，它是程序的入口点  
    public static void main(String []args) {  
        // 定义一个整数变量a，赋值为6  
        int a=6;  
        // 定义一个整数变量b，赋值为3  
        int b=3;  
        // 定义一个整数变量c，赋值为7  
        int c=7;  
        // 如果a大于等于b，则执行以下操作  
        if (a>=b) {  
            // 如果b大于等于c，则执行以下操作  
            if (b>=c) {  
                // 打印a、b、c的排序，中间用"-----"隔开  
                System.out.println(a+"-----"+b+"-----"+c);  
            // 如果b小于c，则执行以下操作  
            }else if (c>=b) {  
                // 如果a大于等于c，则执行以下操作  
                if (a>=c) {  
                    // 打印a、c、b的排序  
                    System.out.println(a+"-----"+c+"-----"+b);  
                // 如果c大于等于a，则执行以下操作  
                }else if (c>=a) {  
                    // 打印c、a、b的排序  
                    System.out.println(c+"-----"+a+"-----"+b);  
                }  
            }  
        // 如果a小于b，则执行以下操作  
        }else if (b>c) {   
            // 这里需要修改一下判断条件，因为现在a<b，所以应该判断b和c的大小关系  
            // 如果b大于c，则执行以下操作  
            System.out.println(b+"-----"+c+"-----"+a); // 这里也需要修改一下输出的顺序，因为现在a<b<c，所以应该先输出b和c的排序，再输出a的排序  
        // 如果c大于b，则执行以下操作  
        }else if (c>b) {   
            // 这里也需要修改一下判断条件，因为现在a<b<c，所以应该判断c和b的大小关系  
            // 如果c大于b，则执行以下操作  
            System.out.println(c+"-----"+a+"-----"+b); // 这里也需要修改一下输出的顺序，因为现在a<b<c，所以应该先输出c和a的排序，再输出b的排序  
        // 如果出现其他情况，即a<b<c的情况，代码将无法处理，这里需要添加处理这种情况的代码。可以使用else分支来处理这种情况。  
        }else {   
            // 在这里输出错误信息，提示这种情况未被处理。可以根据实际情况修改错误信息的输出内容。  
            System.out.println("Error: a<b<c not handled"); // 在这里输出错误信息，提示这种情况未被处理。可以根据实际情况修改错误信息的输出内容。  
        }  
    }  
}
/*先拿出两个数做对比，a和b,从对a和b的赋值判断正确的大小，再运行正确的代码，之所以只有一个结果是因为，这些代码中只有三个if格式是真确的，只打印一个结果
，*/